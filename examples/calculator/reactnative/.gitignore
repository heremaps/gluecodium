# =============================================================================
# Everything in this directory except the files listed at the bottom is
# auto-generated by `reactnative-ops.sh init` and/or `reactnative-ops.sh generate`.
#
# Only three files are hand-crafted and committed:
#   - .gitignore           (this file)
#   - reactnative-ops.sh   (automation script)
#   - README.md            (project documentation)
# =============================================================================

# --- React Native project (created by `reactnative-ops.sh init`) ---
node_modules/
package.json
package-lock.json
index.js
app.json
App.tsx
metro.config.js
babel.config.js
tsconfig.json
jest.config.js
Gemfile
Gemfile.lock
__tests__/
.bundle/
.eslintrc.js
.prettierrc.js
.watchmanconfig
vendor/

# --- Generated by Gluecodium (`reactnative-ops.sh generate`) ---
typescript/
react-native/
cpp/

# --- Android (all created by init) ---
android/

# --- iOS (all created by init) ---
ios/
CalculatorNativeModule.podspec

# --- Build artifacts ---
*.apk
*.aab
*.ipa

# --- Misc ---
.DS_Store
*.hprof
*.log
*.tsbuildinfo
.metro-health-check*
.xcode.env.local
yarn.lock

# Copied C++ implementation (populated by generate step)
native-impl/
