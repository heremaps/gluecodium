/*
 * Copyright (C) 2018 HERE Global B.V. and its affiliate(s). All rights reserved.
 *
 * This software, including documentation, is protected by copyright controlled by
 * HERE Global B.V. All rights are reserved. Copying, including reproducing, storing,
 * adapting or translating, any or all of this material requires the prior written
 * consent of HERE Global B.V. This material also contains confidential information,
 * which may not be disclosed to others without prior written consent of HERE Global B.V.
 *
 */

import "classpath:/TranspilerExtensions.fdepl"
import "./Inheritance.fidl"

define TranspilerExtensions for interface test.ParentInterface
{
    IsInterface = true
}

define TranspilerExtensions for interface test.ChildClass
{
    method createChildClass {
        Static = true
    }
}

define TranspilerExtensions for interface test.GrandchildClass
{
    method createGrandchildClass {
        Static = true
    }
}

define TranspilerExtensions for interface test.RootInterface
{
    IsInterface = true
}

define TranspilerExtensions for interface test.ChildInterface
{
    IsInterface = true
}

define TranspilerExtensions for interface test.AnotherChildInterface
{
    IsInterface = true
}

define TranspilerExtensions for interface test.InheritanceTestHelper
{
    method callRootMethod {
        Static = true
    }

    method createChild {
        Static = true
    }

    method createAnotherChild {
        Static = true
    }

    method createConcreteChild {
        Static = true
    }

    method createAnotherConcreteChild {
        Static = true
    }

    method createConcreteGrandChild {
        Static = true
    }

    method createAnotherConcreteGrandChild {
        Static = true
    }
 }
