{{!!
  !
  ! Copyright (C) 2016-2026 HERE Europe B.V.
  !
  ! Licensed under the Apache License, Version 2.0 (the "License");
  ! you may not use this file except in compliance with the License.
  ! You may obtain a copy of the License at
  !
  !     http://www.apache.org/licenses/LICENSE-2.0
  !
  ! Unless required by applicable law or agreed to in writing, software
  ! distributed under the License is distributed on an "AS IS" BASIS,
  ! WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ! See the License for the specific language governing permissions and
  ! limitations under the License.
  !
  ! SPDX-License-Identifier: Apache-2.0
  ! License-Filename: LICENSE
  !
  !}}
// Auto-generated by Gluecodium. Do not modify.
// React Native callback proxy infrastructure for SDK lambdas.
// Provides thread-safe dispatch of SDK callbacks to JavaScript via CallInvoker.

#pragma once

#include "FfiOpaqueHandle.h"
#include "FfiHandleRegistry.h"
#include <jsi/jsi.h>
#include <react/bridging/CallbackWrapper.h>
#include <memory>

namespace facebook { namespace react { class CallbackWrapper; } }

// Generic C++ base interface for a lambda with N void* parameters.
// The virtual call() signature matches what the SDK expects in its vtable.

template<int N>
class FfiLambdaProxy;

template<int N>
class FfiLambdaProxyImpl;

// ────────────────────────────── 0 parameters ──────────────────────────────
template<>
class FfiLambdaProxy<0> {
public:
    virtual ~FfiLambdaProxy<0>() = default;
    virtual void call() = 0;
};

template<>
class FfiLambdaProxyImpl<0> : public FfiLambdaProxy<0> {
    std::weak_ptr<facebook::react::CallbackWrapper> wrapper_;
public:
    FfiLambdaProxyImpl(std::weak_ptr<facebook::react::CallbackWrapper> w) : wrapper_(std::move(w)) {}
    void call() override {
        auto sw = wrapper_.lock();
        if (!sw) return;
        sw->jsInvoker().invokeAsync([wrapper = wrapper_](facebook::jsi::Runtime& rt) {
            auto sw2 = wrapper.lock();
            if (!sw2) return;
            sw2->callback().call(rt);
            sw2->destroy();
        });
    }
};

// ────────────────────────────── 1 parameter ───────────────────────────────
template<>
class FfiLambdaProxy<1> {
public:
    virtual ~FfiLambdaProxy<1>() = default;
    virtual void call(void* p0) = 0;
};

template<>
class FfiLambdaProxyImpl<1> : public FfiLambdaProxy<1> {
    std::weak_ptr<facebook::react::CallbackWrapper> wrapper_;
public:
    FfiLambdaProxyImpl(std::weak_ptr<facebook::react::CallbackWrapper> w) : wrapper_(std::move(w)) {}
    void call(void* p0) override {
        auto sw = wrapper_.lock();
        if (!sw) return;
        sw->jsInvoker().invokeAsync([wrapper = wrapper_, p0](facebook::jsi::Runtime& rt) {
            auto sw2 = wrapper.lock();
            if (!sw2) return;
            auto a0 = p0 ? facebook::jsi::Value(rt, facebook::jsi::String::createFromUtf8(rt, FfiHandleRegistry::instance().store(p0))) : facebook::jsi::Value::null();
            sw2->callback().call(rt, a0);
            sw2->destroy();
        });
    }
};

// ────────────────────────────── 2 parameters ──────────────────────────────
template<>
class FfiLambdaProxy<2> {
public:
    virtual ~FfiLambdaProxy<2>() = default;
    virtual void call(void* p0, void* p1) = 0;
};

template<>
class FfiLambdaProxyImpl<2> : public FfiLambdaProxy<2> {
    std::weak_ptr<facebook::react::CallbackWrapper> wrapper_;
public:
    FfiLambdaProxyImpl(std::weak_ptr<facebook::react::CallbackWrapper> w) : wrapper_(std::move(w)) {}
    void call(void* p0, void* p1) override {
        auto sw = wrapper_.lock();
        if (!sw) return;
        sw->jsInvoker().invokeAsync([wrapper = wrapper_, p0, p1](facebook::jsi::Runtime& rt) {
            auto sw2 = wrapper.lock();
            if (!sw2) return;
            auto a0 = p0 ? facebook::jsi::Value(rt, facebook::jsi::String::createFromUtf8(rt, FfiHandleRegistry::instance().store(p0))) : facebook::jsi::Value::null();
            auto a1 = p1 ? facebook::jsi::Value(rt, facebook::jsi::String::createFromUtf8(rt, FfiHandleRegistry::instance().store(p1))) : facebook::jsi::Value::null();
            sw2->callback().call(rt, a0, a1);
            sw2->destroy();
        });
    }
};

// ────────────────────────────── 3 parameters ──────────────────────────────
template<>
class FfiLambdaProxy<3> {
public:
    virtual ~FfiLambdaProxy<3>() = default;
    virtual void call(void* p0, void* p1, void* p2) = 0;
};

template<>
class FfiLambdaProxyImpl<3> : public FfiLambdaProxy<3> {
    std::weak_ptr<facebook::react::CallbackWrapper> wrapper_;
public:
    FfiLambdaProxyImpl(std::weak_ptr<facebook::react::CallbackWrapper> w) : wrapper_(std::move(w)) {}
    void call(void* p0, void* p1, void* p2) override {
        auto sw = wrapper_.lock();
        if (!sw) return;
        sw->jsInvoker().invokeAsync([wrapper = wrapper_, p0, p1, p2](facebook::jsi::Runtime& rt) {
            auto sw2 = wrapper.lock();
            if (!sw2) return;
            auto a0 = p0 ? facebook::jsi::Value(rt, facebook::jsi::String::createFromUtf8(rt, FfiHandleRegistry::instance().store(p0))) : facebook::jsi::Value::null();
            auto a1 = p1 ? facebook::jsi::Value(rt, facebook::jsi::String::createFromUtf8(rt, FfiHandleRegistry::instance().store(p1))) : facebook::jsi::Value::null();
            auto a2 = p2 ? facebook::jsi::Value(rt, facebook::jsi::String::createFromUtf8(rt, FfiHandleRegistry::instance().store(p2))) : facebook::jsi::Value::null();
            sw2->callback().call(rt, a0, a1, a2);
            sw2->destroy();
        });
    }
};

// ────────────────────────────── 4 parameters ──────────────────────────────
template<>
class FfiLambdaProxy<4> {
public:
    virtual ~FfiLambdaProxy<4>() = default;
    virtual void call(void* p0, void* p1, void* p2, void* p3) = 0;
};

template<>
class FfiLambdaProxyImpl<4> : public FfiLambdaProxy<4> {
    std::weak_ptr<facebook::react::CallbackWrapper> wrapper_;
public:
    FfiLambdaProxyImpl(std::weak_ptr<facebook::react::CallbackWrapper> w) : wrapper_(std::move(w)) {}
    void call(void* p0, void* p1, void* p2, void* p3) override {
        auto sw = wrapper_.lock();
        if (!sw) return;
        sw->jsInvoker().invokeAsync([wrapper = wrapper_, p0, p1, p2, p3](facebook::jsi::Runtime& rt) {
            auto sw2 = wrapper.lock();
            if (!sw2) return;
            auto a0 = p0 ? facebook::jsi::Value(rt, facebook::jsi::String::createFromUtf8(rt, FfiHandleRegistry::instance().store(p0))) : facebook::jsi::Value::null();
            auto a1 = p1 ? facebook::jsi::Value(rt, facebook::jsi::String::createFromUtf8(rt, FfiHandleRegistry::instance().store(p1))) : facebook::jsi::Value::null();
            auto a2 = p2 ? facebook::jsi::Value(rt, facebook::jsi::String::createFromUtf8(rt, FfiHandleRegistry::instance().store(p2))) : facebook::jsi::Value::null();
            auto a3 = p3 ? facebook::jsi::Value(rt, facebook::jsi::String::createFromUtf8(rt, FfiHandleRegistry::instance().store(p3))) : facebook::jsi::Value::null();
            sw2->callback().call(rt, a0, a1, a2, a3);
            sw2->destroy();
        });
    }
};
