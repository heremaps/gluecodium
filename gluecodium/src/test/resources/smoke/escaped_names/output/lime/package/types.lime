package `package`
types `types` {
    typealias `ULong` = List<`struct`>
    enum `enum` {
        `NaN`
    }
    exception `exception`(`enum`)
    struct `struct` {
        `null`: `enum` = `enum`.`NaN`
    }
    @Dart("Const")
    const `const`: `enum` = `enum`.`NaN`
}
