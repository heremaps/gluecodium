/**
 * Module with helper classes for Transpiler CLI (command-line interface).
 *
 * <h1>Input</h1>
 *
 * For options reader, the input is command line parameters passed to Transpiler when it is run as a
 * command-line tool.
 *
 * <h1>Output</h1>
 *
 * For options reader, the output is an object that contains Transpiler options in parsed form.
 */
package com.here.genium.cli;
