package `package`

import `package`.`types`.`List`
import `package`.`types`.`struct`
import `package`.`types`.`exception`
import `package`.`types`.`enum`

class `class` : interface {
    constructor `constructor`()

    fun `fun`(
        `Double`: `List`
    ): `struct` throws `exception`

    property `property`: `enum` {
        get
        set
    }
}
