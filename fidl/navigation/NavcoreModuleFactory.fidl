package navigation

import navigation.* from "NavigationTypes.fidl"


// Don't generate, write as cpp
interface NavcoreModuleFactory {
  version {
    major 1
    minor 0
  }

  // Needs special impl extending the legacy_ModuleFactoryImpl
}