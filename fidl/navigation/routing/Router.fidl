package navigation.routing

import navigation.routing.* from "RoutingTypes.fidl"
import navigation.routing.* from "Route.fidl"
import navigation.* from "../NavigationTypes.fidl"

interface Router {
  version {
    major 1
    minor 0
  }
}