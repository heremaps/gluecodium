package com.here.navigation

import com.here.navigation.* from "NavigationTypes.fidl"
import com.here.navigation.guidance.* from "./guidance/Guidance.fidl"

// generate
interface NavcoreModule extends legacy_Module { // Needs to extend original
  version {
    major 1
    minor 0
  }

  method createGuidance {
    in {
     GuidanceTypes.MapMatcher legacy_mm
    }
    out {
      GuidanceTypes.Guidance guidance
    }
    error NavigationTypes.Error
  }

  // Reuse impl from legacy
}