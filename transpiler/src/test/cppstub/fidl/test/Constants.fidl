package test

typeCollection TestConstants {
  version {
    major 1
    minor 0
  }

  array Array1 of UInt16

  struct Struct1 {
    Boolean e1
    UInt16 e2
    String e3
  }

  map Map1 {
    UInt16 to String
  }

  const Boolean b1 = true
  const UInt32 maxCount = 10000
  const String foo = "bar"
  const Double pi = 3.1415d

  const UInt32 twentyFive = 5 * 8
  const Boolean b2 = maxCount > 3
  const Boolean b3 = (a && b) || foo == "bar"

  const Array1 empty = []
  const Array1 full = [ 1, 2, 2 + 3, 100 * 100 + 100 ]
  const Struct1 s1 = { e1: true, e2: 1, e3: "foo" }
  const Map1 m1 = [ 1 => "one", 2 => "two" ]
}