package test

import test.sub.TestTypeSubDef.* from  "./sub/TypeSubDef.fidl"

interface TestInterface {
  version {
    major 1
    minor 0
  }
}

typeCollection TestTypeDef {
  version {
    major 1
    minor 0
  }

  typedef TypeOne is UInt8

  typedef TypeTwo is Array1

  typedef TypeThree is TypeTwo

  typedef InstanceId is UInt32 // required for referencing instances via interfaces

  struct Struct2 {
    String[] e0
    Array1 e1
    Struct1 e2
    TypeThree e3
  }

  typedef TypeIncludes is InstanceId // required for referencing TypeIncludes instances via interfaces
  typedef TestInterface is InstanceId // required for referencing TypeIncludes instances via interfaces
}