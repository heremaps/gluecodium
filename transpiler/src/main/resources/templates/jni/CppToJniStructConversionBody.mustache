{
  auto javaClass = env->FindClass("{{>jni/JavaClassSignature}}${{javaClass.name}}");
  auto result = create_object(env, javaClass);
{{#fields}}
  {{#hasCustomType}}
  auto j{{cppField.name}} = convert_to_jni(env, ninput.{{cppField.name}});
  {{/hasCustomType}}
  {{^hasCustomType}}
  auto j{{cppField.name}} = ninput.{{cppField.name}};
  {{/hasCustomType}}
{{/fields}}
{{#fields}}
  {{#hasCustomType}}
  set_object_field(env, javaClass, result, "{{javaField.name}}",
  "L{{>jni/JavaClassSignature}}${{javaClass.name}};", j{{cppField.name}});
  {{/hasCustomType}}
  {{^hasCustomType}}
  set_{{javaField.type.literalName}}_field(env, javaClass, result, "{{javaField.name}}", j{{cppField.name}});
  {{/hasCustomType}}
{{/fields}}
  return result;
}