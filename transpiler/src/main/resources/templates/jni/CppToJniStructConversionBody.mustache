{
  auto javaClass = env->FindClass("L{{>jni/JavaClassSignature}}${{javaClass.name}}");
  auto result = here::internal::create_object(env, javaClass);
{{#fields}}
  {{#hasCustomType}}
  auto j{{cppField.name}} = convert_{{cppField.type.name}}_to_jobject(env, ninput.{{cppField.name}});
  {{/hasCustomType}}
  {{^hasCustomType}}
  auto j{{cppField.name}} = ninput.{{cppField.name}};
  {{/hasCustomType}}
{{/fields}}
{{#fields}}
  {{#hasCustomType}}
  here::internal::set_object_field(env, javaClass, result, "{{javaField.name}}",
  "L{{>jni/JavaClassSignature}}${{javaClass.name}};", j{{cppField.name}});
  {{/hasCustomType}}
  {{^hasCustomType}}
  here::set_{{javaField.type.name}}_field(env, javaClass, result, "{{javaField.name}}", j{{cppField.name}});
  {{/hasCustomType}}
{{/fields}}
  return result;
}