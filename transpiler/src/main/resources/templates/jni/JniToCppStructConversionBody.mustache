{

  jclass javaClass = env->GetObjectClass(jinput);
{{#fields}}
  {{#hasCustomType}}

  convert_from_jni(
    env,
    get_object_field(
    env,
    javaClass,
    jinput,
    "{{javaField.name}}",
    "L{{#owningModel.javaPackages}}{{.}}/{{/owningModel.javaPackages}}{{owningModel.javaClass.name}}${{javaField.type.outerName}};"),
    out.{{cppField.name}} );
  {{/hasCustomType}}
  {{^hasCustomType}}

  out.{{cppField.name}} = get_{{javaField.type.literalName}}_field(env, javaClass, jinput, "{{javaField.name}}");
  {{/hasCustomType}}
{{/fields}}
}
