{{#if type.isInstance}}
  {{type.cppName}} {{name}};
    if (env->IsInstanceOf(j{{name}}, env->FindClass("com/here/android/NativeBase"))) {
        auto long_ptr_{{name}} = get_long_field(env, env->GetObjectClass(j{{name}}), j{{name}}, "nativeHandle");
        {{name}} = *reinterpret_cast<{{type.cppName}}*> (long_ptr_{{name}});
    } else {
        // TODO APIGEN-709: create cpp proxy for java object
    }
{{/if}}
{{#unless type.isInstance}}
{{#if type.isComplex}}
  {{type.cppName}} {{name}};
    here::internal::convert_from_jni( env, j{{name}}, {{name}} );
{{/if}}
{{#unless type.isComplex}}
  {{type.cppName}} {{name}} = j{{name}};
{{/unless}}
{{/unless}}