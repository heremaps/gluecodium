{{>cbridge/FileHeader}}

{{#implementationIncludes}}
{{>common/Include}}
{{/implementationIncludes}}

{{#if selfType}}
void {{name}}_release({{selfType.functionReturnType}} handle) {
    delete get_pointer(handle);
}
{{selfType.functionReturnType}} {{name}}_copy({{selfType.functionReturnType}} handle) {
    return { new (std::nothrow){{selfType}}(*get_pointer(handle)) };
}
{{/if}}

{{#arrays}}
{{>cbridge/ArraysImplementation}}
{{/arrays}}

{{#structs}}
{{>cbridge/StructImplementation}}
{{/structs}}

{{#functions}}
{{>cbridge/FunctionDefinition}}
{{/functions}}

{{#isInterface}}
{{>cbridge/CppProxy}}
{{/isInterface}}
