specification navigation.CppStubSpec
{
  for interfaces {
    IsStruct: Boolean ( default: false );
  }

  for methods {
    Const:   Boolean (default: false);
    Creates: Interface;
  }

  for enumerations {
    IsClass: Boolean ( default: false );
  }

  for arrays {
    IsSet: Boolean (default: false );
  }
}